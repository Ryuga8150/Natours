//- <h1>The Forest Hiker</h1
//- format in pug
//-h1 The Forest Hiker

doctype html
html
  head
    block head
      meta(charset="UTF-8")
      meta(name="viewport" content="width=device-width, initial-scale=1.0")
      //title Natours
      //- using interpolation <---> es6 template strings
      //-title Natours | #{tour}
      
      //-<link rel="stylesheet" href="style.css">
      link(rel="stylesheet" href="/css/style.css")
      link(rel="shortcut icon" type="/image/png" href="/img/favicon.png")
      //-how do the above links get loaded
      //-basically its kind of a like a get request
      //-but we don't have added routes for this

      //-Remeber we made express.static in middleware in app file
      //-so because of that

      link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,300i,700")
      title Natours | #{title}

  body 
    //- //-using the data passed
    //- h1=tour

    //- //- using javascript here
    //- h2=user.toUpperCase()
    //- //- this is also called buffered code as it is retained on web

    //- h1 The Park Camper 
    //- p This is just some text

    //- //-Types of comments
    //- //Visible in Html on web
    //- //-Not visible in Html on web

    //- //- Unbuffered Code which does not provide output
    //- - const x=9
    //- h2=2*x 

    // HEADER
    //<header class="header">  
    //- header.header
    //-     nav.nav.nav--tours
    //-         //-<a href="#" class="nav__el">All tours</a>
    //-         a.nav__el(href="#") All tours
    //-     //For div element only . will work
    //-     .header__logo
    //-         img(src="img/logo-white.png" alt="Natours logo") 
    //-     nav.nav.nav--user
    //-         //- a.nav__el(href="#") My bookings
    //-         //- a.nav__el(href="#")
    //-         //- img.nav__user-img(src="img/user.jpg" alt="User photo") 
    //-         //- span Jonas

    //-         button.nav__el Log in
    //-         button.nav__el.nav__el--cta Sign up

    //- The above content will come from another pug file with _in front of it representing its just for including purposes
    include _header

    // CONTENT
    block content 
      h1 This is a placeholder heading

    //FOOTER
    include _footer

    
    //- script(src self='https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js')
    script(src='/js/mapbox.js')
    //- script(src='/js/login.js')
    script(src='https://js.stripe.com/v3/')
    script(src="/js/bundle.js")
    //-script(src self ="https://js.stripe.com/v3/")